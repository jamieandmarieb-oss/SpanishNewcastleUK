{
  "name": "sml3004-canvas-lti-prototype",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Canvas LTI 1.3 homework-submission wrapper for the SML3004 video dialogue.",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check lti-client.js && node --check scripts/generate-keys.mjs",
    "generate-keys": "node scripts/generate-keys.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.2.1",
    "jose": "^6.2.4",
    "multer": "^2.2.0"
  }
}
